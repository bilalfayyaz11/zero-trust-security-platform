[Unit]
Description=oauth2-proxy identity-aware access gateway
After=network.target

[Service]
ExecStart=/usr/local/bin/oauth2-proxy --config /etc/oauth2-proxy.cfg
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
